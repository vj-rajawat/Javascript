//Reduce function example
// function changCar(event) {
    // let carId = event.target.value;
    // let slug = car.slug;
    // console.log(carId);
    // fetch('http://galaxy.test/api/car/' + carId)
        // .then(response => response.json())
        // .then(car => {
            // selectedCar = car;
            // console.log(car)
            // let str = car.variants.reduce((innerHTML, variant) => innerHTML +
                // `<option value='${variant.id}'>${variant.name}</option>`, '')
            // let str = "<option value='abc'>abc</option>";
            // console.log(str)

            // document.getElementById('image').src = car.media[0].original_url;
            // document.getElementById('car_variant').innerHTML = str;
